<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/legacy_nav_graph"
    app:startDestination="@id/convertFragment">

    <fragment
        android:id="@+id/convertFragment"
        android:name="com.juan.legacy_ui.fragment.ConvertFragment"
        android:label="Conversor de monedas">
        <action
            android:id="@+id/action_convert_to_history"
            app:destination="@id/historyFragment" />
    </fragment>

    <fragment
        android:id="@+id/historyFragment"
        android:name="com.juan.legacy_ui.fragment.HistoryFragment"
        android:label="Historial de conversiones">
        <action
            android:id="@+id/action_history_to_detail"
            app:destination="@id/detailFragment">
            <argument
                android:name="id"
                app:argType="string" />
        </action>
    </fragment>

    <fragment
        android:id="@+id/detailFragment"
        android:name="com.juan.legacy_ui.fragment.DetailsFragment"
        android:label="Detalle de conversión">
        <argument
            android:name="id"
            app:argType="string" />
    </fragment>

</navigation>